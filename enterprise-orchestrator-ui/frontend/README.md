# Frontend (Planned)

This folder will host the URL-based UI.

Planned pages:
- Submit job (job_name/table_name/repo)
- Job status and live updates
- Artifact links (SharePoint first, Confluence later)
- Audit history and filters (Phase 2)

Suggested stack:
- Next.js + TypeScript
- React Query for API polling
- Server-side auth integration (Phase 2)
